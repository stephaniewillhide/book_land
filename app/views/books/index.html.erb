<%= link_to("New", new_book_path) %>

<table>
  <thead>
    <th>Name</th>
    <th>ISBN</th>
    <th>Actions</th>
  </thead>
  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td><%= book.name %></td>
        <td><%= book.isbn %></td>
      </tr>
    <% end %>
  </tbody>
</table>
